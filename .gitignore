# Environment-specific configs (secrets + env-specific paths)
config/config.dev.yaml
config/config.test.yaml
config/config.prod.yaml

# Docker secrets
.env

# Python
lh_env_312/
__pycache__/
*.pyc
*.egg-info/

# Spark / Iceberg local artifacts
/tmp/
*.crc
_SUCCESS

# Large/binary dependencies — document how to obtain in README instead
jars/*.jar

# Data files (often large / contain real data, not source code)
data/
data_test/
data_prod/

# OS
.DS_Store
